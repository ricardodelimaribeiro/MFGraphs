#!/usr/bin/env wolframscript
(* LEGACY_SOLVER_REPRO: disabled.

   This repro depended on removed legacy archive modules. The active workflow uses examples.wl, makeScenario,
   makeSystem, and reduceSystem/dnfReduceSystem instead. *)

Print["test_legacy_inconsistent.wls is disabled: it depended on removed legacy archive files."];
Exit[0];
