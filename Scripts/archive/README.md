# Scripts Archive

This directory contains solver-era and graphics-era scripts that are intentionally
outside the active core-only workflow.

## Scope

- Benchmark and profiling runners tied to solver modules.
- DNF and critical-solver instrumentation scripts.
- Legacy exact-mode and feasibility phase-diagram scripts.

## Active workflow

Use `Scripts/RunTests.wls fast` and core tests in `MFGraphs/Tests/`.
