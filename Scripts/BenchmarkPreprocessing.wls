#!/usr/bin/env wolframscript
(* LEGACY_SOLVER_SCRIPT: disabled.

   This benchmark compared active linear preprocessing against TripleClean
   from the removed DataToEquations archive module. It is retained as a
   historical entry point, but is not runnable in the active scenario-kernel
   workflow. *)

Print["BenchmarkPreprocessing.wls is disabled: it depended on removed DataToEquations legacy helpers."];
Exit[0];
